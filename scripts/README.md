# Reproduction Scripts

The following executable scripts must be added from the verified implementation before public release:

- training entry point
- validation and metric-evaluation entry point
- image inference entry point
- TensorRT export and FP16 inference entry point
- desktop and Jetson FPS measurement scripts
- result aggregation script

Do not add placeholder code that does not reproduce the reported model.
